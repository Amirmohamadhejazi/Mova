import { type ReactNode } from 'react'
import { type DefaultMantineColor } from '@mantine/core'

interface IDChipProps {
    children: ReactNode
    color: DefaultMantineColor
    className?: string
}

export default IDChipProps
